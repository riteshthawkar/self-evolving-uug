# Copy values into a local .env or export them in your shell.
# Do not commit real tokens or API keys.

HF_TOKEN=
OPENAI_API_KEY=
WANDB_API_KEY=

HF_HOME=${HOME}/.cache/huggingface
TRANSFORMERS_CACHE=${HF_HOME}/transformers
HF_DATASETS_CACHE=${HF_HOME}/datasets
