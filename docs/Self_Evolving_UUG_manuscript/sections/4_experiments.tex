\section{Experiments}
\label{sec:experiments}

We evaluate the self-evolving framework along five axes: whether it consistently improves both understanding and generation across architecturally distinct models (Sec.~\ref{sec:main_results}), how it compares to existing self-supervised post-training methods under equivalent supervision constraints (Sec.~\ref{sec:comparison}), which components are essential to its effectiveness (Sec.~\ref{sec:ablation}), whether the training dynamics exhibit the expected curriculum escalation and capability progression (Sec.~\ref{sec:dynamics}), and how the improvements manifest qualitatively in generated content and question complexity (Sec.~\ref{sec:qualitative}).

\subsection{Experimental Setup}
\label{sec:setup}

% TODO: Fill with actual experimental details once training is complete.

\noindent\textbf{Models.}
We apply the framework to three unified understanding and generation models that employ fundamentally different image synthesis mechanisms: BLIP3o-8B (diffusion-based denoising), BAGEL (flow-matching with a variational autoencoder), and VARGPT (autoregressive discrete token generation). For each model, we attach three LoRA adapters (proposer, solver, generator) to the frozen backbone with rank $r$ and train them using identical hyperparameters and training schedules.

\noindent\textbf{Training data.}
All training uses unlabeled images only. No captions, question-answer pairs, or human annotations are used at any stage. The image sources and quantities are reported per model.

\noindent\textbf{Understanding benchmarks.}
We evaluate visual understanding on MMMU, MMBench, TextVQA, SEED-Bench, RealWorldQA (RWQA), MM-Vet, and both perception (MME-P) and cognition (MME-C) splits of MME.

\noindent\textbf{Generation benchmarks.}
We evaluate image generation on GenEval (compositional attribute accuracy), DPG-Bench (dense prompt following), and WISE (open-ended generation quality).

\noindent\textbf{Training details.}
% TODO: Fill with learning rate, batch size, number of steps, LoRA rank,
% number of prompt perturbations N, STE window size, U:G step ratio,
% and other hyperparameters.


Table~\ref{tab:qualitative_comparison} provides a structured qualitative comparison of our framework against existing post-training methods along the five design axes introduced in Section~\ref{sec:related_work}. Our approach is the only method that simultaneously requires no external supervision, optimizes both understanding and generation bidirectionally, employs multiple complementary reward signals, incorporates explicit stability controls, and demonstrates generality across three architecturally distinct model families.

\input{tables/qualitative_comparison}

\subsection{Main Results}
\label{sec:main_results}

Table~\ref{tab:main_results} presents understanding and generation results across all three backbone architectures. Existing unified understanding and generation models (Janus-Pro-7B, SEED-X, Emu3) are included as reference points to contextualize the performance landscape. Each of our models is evaluated in its pretrained base configuration and after applying the self-evolving framework, with the absolute improvement ($\Delta$) reported for each benchmark.

% TODO: Add discussion of results once numbers are available.
% Key points to cover:
% - Consistent improvement on both understanding AND generation across all 3 models
% - How our post-trained models compare to other unified models
% - Highlight that the same algorithm/hyperparameters work across diffusion, flow-matching, and autoregressive
% - Note any benchmark where improvements are particularly large or small, and why
% - Generation Avg column provides a single summary statistic

\input{tables/quantitative_tables}


\subsection{Comparison with Self-Supervised Methods}
\label{sec:comparison}

Table~\ref{tab:selfsup_comparison} provides a comprehensive comparison organized into three groups. Base unified models, including the backbones used by competing methods and our own three architectures, establish the starting point before any post-training. Self-supervised post-training methods, which operate without curated labels, supervised fine-tuning data, or external reward models, constitute the fair comparison group. Our results are reported across all three backbones to demonstrate that the improvements are not specific to a single architecture. Methods that employ additional supervision are reported separately in the supplementary material as reference upper bounds.

% TODO: Add discussion once numbers are filled.
% Key points to cover:
% - How our method compares to each self-supervised baseline
% - Highlight that we achieve competitive or superior results without ANY external signal
% - Note backbone differences where relevant (our BLIP3o-8B vs their Janus-Pro-7B, etc.)
% - Our three base models serve as direct ablation: base vs ours shows the effect of our framework
% - Reference supplementary Table S1 for supervised comparison


\subsection{Ablation Study}
\label{sec:ablation}

We ablate each framework component on BLIP3o-8B to isolate its contribution. Table~\ref{tab:ablation} reports understanding performance (averaged across MMMU, MMBench, SEED, and MMVet) and generation performance (GenEval) for each configuration.

% TODO: Add discussion once numbers are filled.
% Key points to cover:
% - Understanding signal ablations: STE vs self-consistency, importance of prompt perturbation
% - Generation signal ablations: QA fidelity vs cycle consistency complementarity
% - Optimization ablations: GRPO vs REINFORCE, bucket-stratified baselines
% - Architecture ablations: joint vs understanding-only vs generation-only
%   (this directly tests whether the two loops benefit each other)


\subsection{Training Dynamics}
\label{sec:dynamics}

Figure~\ref{fig:dynamics} visualizes the evolution of key training signals over the course of optimization, providing direct evidence that the self-evolving loop operates as intended.

\begin{figure*}[tb]
  \centering
  % TODO: Replace with actual training dynamics plots.
  % Recommended layout: 2x2 grid of subplots.
  % \includegraphics[width=\linewidth]{figures/training_dynamics.pdf}
  \fbox{\parbox{0.95\linewidth}{\centering\vspace{3.5cm}\textbf{[Training Dynamics Figure Placeholder]}\\\smallskip
  Four subplots: (a) Mean STE difficulty over training steps, (b) Self-consistency entropy distribution,\\
  (c) Easy/Medium/Hard bucket proportions, (d) Solver validation accuracy.\vspace{3.5cm}}}
  \caption{Training dynamics of the self-evolving loop on BLIP3o-8B. (a) Mean STE difficulty increases steadily as the proposer learns to generate harder questions, confirming progressive curriculum escalation. (b) Self-consistency entropy shifts from a zero-dominated distribution toward higher values as the proposer targets the solver's competence boundary. (c) The proportion of questions in the easy bucket decreases over training while medium and hard buckets grow, demonstrating effective difficulty calibration. (d) Solver validation accuracy on held-out benchmarks improves monotonically, confirming that harder questions translate to genuine capability gains rather than spurious difficulty inflation.}
  \label{fig:dynamics}
\end{figure*}

% TODO: Add discussion once plots are generated.
% Key points to cover:
% - (a) STE difficulty increases: proposer generates harder questions over time
% - (b) Entropy distribution shifts: early training dominated by H_sc=0, later more spread
% - (c) Bucket proportions: easy decreases, medium/hard increase — curriculum works
% - (d) Solver accuracy: monotonic improvement validates that difficulty escalation is productive
% - Optionally mention generation reward trend if space allows


\subsection{Qualitative Analysis}
\label{sec:qualitative}

Figure~\ref{fig:qualitative} presents representative examples that illustrate the framework's impact on both understanding and generation quality.

\begin{figure*}[tb]
  \centering
  % TODO: Replace with actual qualitative comparison figure.
  % Recommended layout: Top row = generation (real image, base generated, ours generated, prompt),
  %                     Bottom row = proposer question evolution (early vs late training).
  % \includegraphics[width=\linewidth]{figures/qualitative_results.pdf}
  \fbox{\parbox{0.95\linewidth}{\centering\vspace{4cm}\textbf{[Qualitative Results Figure Placeholder]}\\\smallskip
  Top: Generation comparison (prompt $\rightarrow$ base model output vs.\ ours) across diverse scenes.\\
  Bottom: Proposer question evolution showing early-stage simple questions vs.\ late-stage complex questions.\vspace{4cm}}}
  \caption{Qualitative comparison of generation and understanding before and after self-evolving training. Top two rows: given a text prompt, we compare images generated by the base model and our self-evolved model across three architectures. The self-evolved models produce images with more accurate attribute binding, improved spatial coherence, and better prompt adherence. Bottom row: proposer question evolution from early to late training, showing progression from simple object identification questions to compositionally complex queries that probe spatial relationships, counting, and contextual reasoning.}
  \label{fig:qualitative}
\end{figure*}

% TODO: Add discussion once qualitative examples are collected.
% Key points to cover:
% - Generation: specific improvements in attribute accuracy (color, count, spatial layout)
% - Generation: holistic scene coherence improvements
% - Proposer evolution: concrete examples of question complexity increasing
% - Optionally: 1-2 failure cases showing current limitations
