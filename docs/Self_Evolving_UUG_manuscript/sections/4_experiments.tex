\section{Experiments}
\label{sec:experiments}

\subsection{Experimental Setup}
\label{sec:setup}

\noindent\textbf{Models.}
We apply the framework to three unified models with different synthesis mechanisms: BLIP3o-8B~\cite{chen2025blip3o} (diffusion denoising), BAGEL~\cite{deng2025bagel} (flow matching), and VARGPT$_{1.1}$~\cite{zhuang2025vargpt} (autoregressive token generation, Qwen backbone). For each model, we attach three LoRA adapters (Proposer, Solver, Generator) to a frozen backbone and train with the same self-evolving recipe.

\noindent\textbf{Training data.}
All training uses unlabeled images only. No captions, question-answer pairs, or human annotations are used at any stage.

\noindent\textbf{Benchmarks.}
We evaluate understanding on MMMU, MMBench, TextVQA, SEED-Bench, RealWorldQA (RWQA), MM-Vet, and both MME perception (MME-P) and cognition (MME-C). For generation, we use GenEval and report Single Object, Two Objects, Counting, Colors, Position, and Color Attribution.

\noindent\textbf{Training details.}
Unless otherwise stated, we use the strict final configuration. We train for 10{,}000 steps in bfloat16 with learning rate $1\times10^{-6}$, weight decay 0.01, and gradient clipping 1.0. LoRA uses rank 16, $\alpha=32$, and dropout 0.05 on attention/MLP projection modules. Solver self-consistency uses $N=7$ prompt perturbations, the Proposer samples 3 candidates per step, and generation samples 3 candidate images per prompt. For BLIP3o, generation uses 896$\times$896 resolution, 50 denoising steps, and guidance scale 2.0. The joint run uses a 3:2 understanding:generation cycle. Understanding-only and generation-only ablations use 5:0 and 0:5; the imageless loop uses 3:2 with replay-buffer-only understanding updates. Base and self-evolved checkpoints are evaluated with identical inference settings within each backbone.


\subsection{Main Results}
\label{sec:main_results}

Tables~\ref{tab:understanding_results} and~\ref{tab:generation_results} present understanding and generation results across unified models, prior self-evolving/post-training methods, and our framework on three architecturally distinct backbones.

Compared to each base checkpoint, our framework improves both capabilities on all three backbones. On BLIP3o, MMMU improves from 50.6 to 52.8, MMBench from 83.5 to 86.1, TextVQA from 83.1 to 85.2, and GenEval from 0.84 to 0.87. On BAGEL, MMMU improves from 55.3 to 58.8, MMBench from 85.0 to 87.1, MMVet from 67.2 to 69.5, and GenEval from 0.82 to 0.85. On VARGPT$_{1.1}$, MMMU improves from 48.6 to 51.6, MMBench from 81.0 to 83.7, TextVQA from 82.0 to 84.8, SEED from 76.1 to 79.2, and GenEval from 0.53 to 0.56.

Category-level generation deltas are not uniformly positive: some GenEval subcategories remain flat or slightly decrease while overall GenEval improves. This indicates controlled trade-offs rather than mode collapse. Transfer across diffusion, flow-matching, and autoregressive models supports the model-agnostic objective.

Compared to prior self-evolving methods, our framework is competitive while providing broader architectural transfer evidence. Most prior methods are evaluated on a single backbone with partial benchmark overlap (e.g., UniGame, SUDER, UniRL, UniCorn~\cite{su2025unigame,hong2025dualselfrewards,mao2025unirl,han2026unicorn}). We therefore treat cross-paper raw-score comparisons as reference only and prioritize within-backbone base-vs-ours gains.

\input{tables/table_understanding}

\FloatBarrier

\input{tables/table_generation}

\FloatBarrier


\subsection{Analysis}
\label{sec:analysis}

\paragraph{Training dynamics.}
Figure~\ref{fig:training_dynamics} traces the evolution of understanding and generation metrics over training steps for all three backbones.

\begin{figure*}[!htb]
  \centering
  \includegraphics[width=\linewidth]{figures/training_dynamics.pdf}
  \caption{\textbf{Training dynamics} across three architecturally distinct backbones (diffusion, flow matching, autoregressive). \textbf{Top row:} understanding signals --- \emph{proposer reward} (quality of self-generated questions), \emph{SC entropy} (self-consistency disagreement among $N{=}7$ prompt perturbations), and \emph{STE difficulty} (Solver Token Entropy, a token-level difficulty proxy). Proposer reward and SC entropy increase as the Proposer learns to generate harder, more informative questions. STE difficulty remains approximately stable throughout training, confirming it serves as a stationary reference signal rather than a drifting curriculum target. \textbf{Bottom row:} generation signals --- \emph{total reward} (weighted combination of generation assessment signals), \emph{QA fidelity} (attribute-level accuracy via Solver re-verification), and \emph{cycle consistency} (caption$\to$image$\to$caption round-trip coherence). Generation improvement is delayed relative to understanding as the generation loop requires warm-up before producing useful training signal. All three backbones show qualitatively similar trends, supporting the model-agnostic design.}
  \label{fig:training_dynamics}
\end{figure*}

\paragraph{Signal analysis.}
Figure~\ref{fig:signal_analysis} visualizes the behavior of the proposer reward components and the generation assessment signals over training.

\begin{figure}[!htb]
  \centering
  \includegraphics[width=\linewidth]{figures/signal_analysis.pdf}
  \caption{\textbf{Signal analysis} on BLIP3o-8B. (a)~\emph{STE curriculum progression:} the distribution of Solver Token Entropy difficulty shifts gradually rightward over training, indicating that the Proposer learns to generate progressively harder questions as the Solver improves. (b)~\emph{Complementary difficulty dimensions:} STE and self-consistency (SC) entropy capture orthogonal aspects of question difficulty. Four quadrants emerge --- \emph{token-hard} (high STE, low SC): questions with difficult tokens but consistent solver answers; \emph{informative} (high both): boundary-level questions that maximise learning signal; \emph{trivial} (low both): easy questions filtered out by the curriculum; \emph{framing-hard} (low STE, high SC): questions where prompt framing causes solver disagreement. Dashed lines mark medians. (c)~\emph{Generation reward components:} QA fidelity (attribute-level accuracy) and cycle consistency (holistic coherence) both improve, with fidelity rising faster. Generation signals are delayed relative to understanding due to the warm-up phase needed for stable generation rollouts.}
  \label{fig:signal_analysis}
\end{figure}

\paragraph{Qualitative analysis.}
Figure~\ref{fig:qualitative} illustrates both self-supervision mechanisms during training. The top panel shows self-consistency correction for understanding: a single greedy pass produces an incorrect answer (red), while majority voting across $N{=}7$ prompt perturbations recovers the correct answer (green). The bottom panel shows QA-fidelity failure detection for generation: the Solver detects counting errors, semantic mismatches, and color-attribution failures. Together, these examples show that both loops produce discriminative signals without external reward models.

\begin{figure*}[!htb]
  \centering
  \includegraphics[height=0.56\textheight,keepaspectratio]{figures/qualitative_comparison.pdf}
  \caption{Qualitative analysis. \textbf{Top:} self-consistency correction in understanding. \textbf{Bottom:} QA-fidelity error detection in generation (BAGEL). Green indicates pass; red indicates fail.}
  \label{fig:qualitative}
\end{figure*}


\subsection{Ablation Study}
\label{sec:ablation}

We ablate each framework component on BLIP3o-8B. Tables~\ref{tab:ablation} and~\ref{tab:loop_ablation} report per-benchmark understanding and generation performance for each configuration.

\input{tables/table_ablation}

\FloatBarrier

\input{tables/table_loop_coupling}

\FloatBarrier

\paragraph{Understanding signal ablations.}
Removing STE while keeping self-consistency reduces understanding performance, confirming that STE resolves signal degeneracy. Without STE, the Proposer cannot separate trivial questions from boundary-level ones when Solver responses all agree, which weakens curriculum progression. Removing self-consistency while keeping STE also reduces performance, showing complementary value. Replacing prompt perturbation with temperature sampling gives the largest degradation, indicating that semantic framing diversity is more informative than decoding noise.

\paragraph{Generation signal ablations.}
Removing QA fidelity or cycle-consistent captioning degrades generation, but with different failure modes. Without QA fidelity, the Generator can satisfy global coherence while missing fine-grained attributes. Without cycle consistency, it can satisfy individual diagnostic questions while producing holistically incoherent scenes. These ablations confirm that both signals are complementary.

\paragraph{Loop coupling analysis.}
Table~\ref{tab:loop_ablation} and Figure~\ref{fig:loop_coupling} isolate coupling between understanding and generation. Understanding-only training improves understanding but not generation. Generation-only training improves generation moderately but does not improve understanding. The full framework exceeds both single-loop settings, confirming that the Solver's dual role couples the two directions. Replacing external unlabeled images with self-generated images further tests self-sufficiency: the understanding loop then trains only on Generator outputs, closing a fully autonomous cycle.

\begin{figure}[!htb]
  \centering
  \includegraphics[width=\linewidth]{figures/loop_coupling.pdf}
  \caption{\textbf{Loop coupling analysis} on BLIP3o-8B. (a)~Understanding performance and (b)~generation performance measured as relative improvement over the base checkpoint (higher is better). \emph{Full framework} (blue solid) trains both understanding and generation loops jointly. \emph{Understanding only} / \emph{generation only} (dashed) train a single loop in isolation. The opposite-loop baseline (dotted at zero) receives no training signal for that capability. The full framework consistently outperforms either single-loop ablation on \emph{both} tasks, with the shaded \emph{synergy gap} showing the additional improvement attributable to cross-loop coupling through the Solver's dual role. Generation performance benefits more from coupling than understanding, as the Solver's question-answering ability directly improves generation assessment quality.}
  \label{fig:loop_coupling}
\end{figure}
